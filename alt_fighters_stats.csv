import pandas as pd
import numpy as np
from scipy import stats
import matplotlib
matplotlib.use("Agg")
import matplotlib.pyplot as plt

plt.rcParams["font.sans-serif"] = ["IPAexGothic", "Noto Sans CJK JP", "DejaVu Sans"]
plt.rcParams["axes.unicode_minus"] = False

NAVY = "#1E2761"
BLUE = "#0070C0"
RED = "#990011"
CARD_BORDER = "#BFBFBF"
STANCE_COLORS = {"Orthodox": NAVY, "Southpaw": RED, "Switch": BLUE}
style_order = ["ストライカー", "オールラウンダー", "グラップラー"]
main_stances = ["Orthodox", "Southpaw", "Switch"]

m = pd.read_csv("style_trueskill_merged.csv").rename(columns={"stance_x": "stance"})
m = m[m["stance"].isin(main_stances)]

pv_mean = m.pivot_table(index="style_label", columns="stance", values="conservative", aggfunc="mean").reindex(style_order)
pv_sem = m.pivot_table(index="style_label", columns="stance", values="conservative", aggfunc="sem").reindex(style_order)
pv_n = m.pivot_table(index="style_label", columns="stance", values="conservative", aggfunc="count").reindex(style_order)

fig, ax = plt.subplots(figsize=(7.6, 5.8))
fig.patch.set_facecolor("white"); ax.set_facecolor("white")
x = np.arange(len(style_order))
for i, stance in enumerate(main_stances):
    offset = (i - 1) * 0.06
    ax.errorbar(x + offset, pv_mean[stance].values, yerr=pv_sem[stance].values,
                marker="o", markersize=9, linewidth=2.4, capsize=5,
                color=STANCE_COLORS[stance], label=stance, alpha=0.9)

ax.set_xticks(x)
ax.set_xticklabels(style_order, fontsize=12)
ax.set_xlim(-0.4, 2.4)
ax.set_ylabel("平均 TrueSkill（保守的評価値 μ−3σ）", fontsize=12, color="#262626")
ax.set_title("スタンス × 得意分野：交互作用プロット（エラーバー＝標準誤差）\n二元配置ANOVA：交互作用 p=0.283（統計的に非有意）",
             fontsize=13.5, fontweight="bold", color=NAVY, pad=12)
ax.legend(loc="upper left", fontsize=11, frameon=True, facecolor="white", edgecolor=CARD_BORDER)
ax.grid(alpha=0.25, color=CARD_BORDER)
for spine in ["top", "right"]:
    ax.spines[spine].set_visible(False)
for spine in ["left", "bottom"]:
    ax.spines[spine].set_color(CARD_BORDER)
fig.tight_layout()
fig.savefig("interaction_stance_style_jp.png", dpi=200, facecolor="white")
plt.close(fig)
print("saved interaction_stance_style_jp.png (with error bars)")
